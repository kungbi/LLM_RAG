# Base image is the official Ollama image
FROM ollama/ollama:latest

# Install the desired model
RUN ollama pull qwen2:1.5b

# Expose the same port as in the original configuration
EXPOSE 11434

# Start Ollama server (this is the default command for the original image)
# CMD ["ollama", "pull", "qwen2:1.5b"]
